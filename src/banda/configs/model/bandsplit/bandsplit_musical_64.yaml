

# Bandsplit Configuration
band_specs:
  
  bandsplit_type: musical # Changed to string value
  n_bands: 64
require_no_overlap: False
require_no_gap: True
normalize_channel_independently: False
treat_channel_as_feature: True