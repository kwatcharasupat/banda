defaults:
  - _self_
  - loss: default
  - metrics: default
  - optimizer: default
  - trainer: default
  - data: default # Added data default
  - logger: wandb # Moved from experiment configs
  - model: default # Added model default

project_root: ${oc.env:PROJECT_ROOT,.}
experiment_name: bandit_musdb18hq_test
seed: 42
log_level: INFO

paths:
  output_dir: ${hydra.run.dir}
  log_dir: ${hydra.run.dir}/logs
  data_root: ${oc.env:DATA_ROOT,/Users/kwatcharasupat/personal/data}